#include <stdio.h>

/**
 * main - a program that prints the number of arguments passed
 * into it including a new line.
 *
 *
 * Return Always 0.
 */

int main(int argc, char* arg[]__attribute__((unused)))
{
	printf("%d\n", argc - 1);

		return (0);

}
