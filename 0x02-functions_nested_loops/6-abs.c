#include "main.h"
/**
 *  _abs(intn) - checks for absolute value
 *
 * i: input number as an integer
 *
 * Return: Absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
